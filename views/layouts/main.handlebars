<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	 <!--Import Google Icon Font-->
	<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
	<!--Import materialize.css-->
	<link rel="stylesheet"
		href="https://cdn.jsdelivr.net/npm/@materializecss/materialize@2.0.1-alpha/dist/css/materialize.min.css">
	<link rel="stylesheet" type="text/css" href="../../Public/css/style.css"/>
	<title>Restaurant Express</title>
</head>

<body>
	<header>
		<nav>
			<div class="nav-wrapper">
				<a href="./main" class="brand-logo left margin-left-60">Restaurant Express</a>
				<img src="../../Public/images/logo.png" class="brand-logo left opacity-80" width="50">
				<ul id="nav-mobile" class="right">
					{{#if logged_in}}
					<li><button class="btn" id="logout">Logout</button></li>
					{{else}}
					<li><button class="btn" id="createAccount">Create Account</button></li>
					<li><button class="btn" id="Login">Login</button></li>
					{{/if}}
				</ul>
			</div>
		</nav>
	</header>
  
	<main>
		
		{{#if logged_in}}
			{{{ body }}}

		{{else}}
		<div class="center margin-10">
			<img src="../../Public/images/logo.png" id="landing-img">
		</div>
		{{/if}}

	</main>

	{{#if logged_in}}
	<script src="../../Public/js/logout.js"></script>
	{{/if}}

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
	<script src="https://cdn.jsdelivr.net/npm/@materializecss/materialize@2.0.1-alpha/dist/js/materialize.min.js"></script>
</body>
</html>